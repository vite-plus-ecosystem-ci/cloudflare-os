{
  "name": "@gadgets/workshop-evals",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test:run": "vp test run --config vitest.config.ts",
    "test:watch": "vp test --config vitest.config.ts",
    "evals": "pnpm exec vp run -F @gadgets/integration-tests build:test-gatekeeper && vp test run --config vitest.eval.config.ts --reporter=vitest-evals/reporter --reporter=json --outputFile.json=.wrangler/evals/results.json",
    "evals:ui": "vitest-evals serve .wrangler/evals/results.json"
  },
  "dependencies": {
    "@gadgets/integration-tests": "workspace:*",
    "@gadgets/workshop-shared": "workspace:*",
    "capnweb": "catalog:",
    "vitest-evals": "0.16.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "^26.1.0",
    "chess.js": "1.4.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
